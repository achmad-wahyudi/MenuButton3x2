<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_main"
    android:layout_width="match_parent"
    android:layout_height="@dimen/menu_item"
    android:layout_margin="8dp"
    android:background="@drawable/round_item_white"
    android:clickable="true"
    android:elevation="3dp"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_menu"
        android:layout_width="@dimen/menu_item_img"
        android:layout_height="@dimen/menu_item_img"
        android:layout_margin="2dp"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        tools:src="@drawable/ic_manue_radio_presenter_outline" />

    <TextView
        android:id="@+id/tv_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="@dimen/menu_item_text"
        tools:text="Time Attendance" />
</LinearLayout>